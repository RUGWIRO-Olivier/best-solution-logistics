package com.bestSolutionCargo.repository;

import com.bestSolutionCargo.models.SpareParts;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SparePartsRepo extends JpaRepository<SpareParts,String> {
}
